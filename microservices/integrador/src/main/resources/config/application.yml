spring:
  application.name: integrador
  jmx.enabled: false

  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    username: sa_integrador_p12t
    password: 1gYQkNqgVt57pakn
    hikari:
      pool-name: ${spring.application.name}
      maximum-pool-size: 5
      connection-timeout: 60000
    jdbc-url: jdbc:oracle:thin:@ptioracledev01.fpti.pti.org.br:1521:totvsdev

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.Oracle12cDialect

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  servlet:
    session:
      persistent: false

management:
  health:
    ldap:
      enabled: false
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: ['health', 'info', 'flyway' , 'logfile']
  endpoint:
    health:
      enabled: true
    info:
      enabled: true
    flyway:
      enabled: true
    logfile:
      enabled: true
      external-file: /var/log/${spring.application.name}/${spring.application.name}.log

logging:
  file:
    name: /var/log/${spring.application.name}/${spring.application.name}.log
  level:
    root: INFO
    org.hibernate: INFO
    org.springframework: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.context.support: INFO

info:
  name: integrador
  description: 'Integrador de servicos para o ERP Protheus'
  version: @project.version@-@project.build-number@
  environment: @active.profile@

# configs de acesso ao ws protheus
protheus:
  api:
    rest: http://ptiprotheus12t:8080/api/
