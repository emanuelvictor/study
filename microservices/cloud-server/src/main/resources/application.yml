# configs gerais
spring:
  application:
    name: eureka-server
  profiles:
    active: @active.profile@

# configs do server
server:
  port: 8761

# configs do Eureka
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
